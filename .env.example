# Host / Vercel environment variables. Never commit real values.
FREE_BETA_MODE=true
# Initial default only (1-6). Operator dashboard stores the live daily target.
DAILY_PICK_TARGET=3
CRON_SECRET=
ODDS_API_KEY=
DISCORD_WEBHOOK_URL=
DATABASE_URL=
BOATBOYZ_PIN=

# GitHub Actions secrets only (not needed on Vercel):
# APP_URL=https://your-app.vercel.app
# CRON_SECRET=  (same value as CRON_SECRET above)
